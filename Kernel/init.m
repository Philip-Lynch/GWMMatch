(* Kernel/init.m *)
(* Autoload entry point for the GWMMatch package. *)
(* Place the GWMMatch/ folder in $UserBaseDirectory/Applications/ and *)
(* then load with:  << GWMMatch`  *)

Get[FileNameJoin[{DirectoryName[$InputFileName], "..", "GWMMatch.wl"}]]
